# 🙀 Translator Beta 변경 로그

## v1.0.5-beta.6 — 번역문 수기 편집 저장 경합 수정

- 🐟/🍖 번역문 편집에서 문장이나 마지막 줄을 삭제한 뒤 저장해도 기존 번역이 되살아나던 문제 수정
- 번역문 편집 중에는 해당 메시지의 `MESSAGE_EDITED` 및 원문 오염 자동복구 처리를 잠시 보류
- 저장 버튼을 누르는 순간 한국어 수정본을 별도 확보한 뒤 `display_text`와 현재 스와이프 번역에 우선 저장
- SillyTavern이 저장 과정에서 메시지 객체를 교체하거나 영어 원문을 먼저 복구해도 수정본 유지
- 편집 취소 시 기존 번역 복원, 채팅·스와이프 변경 시 낡은 수정본 폐기 동작 유지
- 실제 제보 순서인 `마지막 대사 삭제 → 저장 → 오염 방어 선행` 회귀 테스트 추가
- 번역 엔진, 프롬프트, 형식 검증, 캐시 판정은 변경하지 않음

beta.5는 번역 자체와 코드블럭 검증은 정상이나 번역문 수기 편집 저장이 되돌아갈 수 있으므로 beta.6로 교체하세요.

---

## v1.0.5-beta.5 — 코드블럭 언어별 검증·구조 키 자동복구

- Markdown/커스텀 정보블럭의 시간 `10:52`, 문장 중간 `Main:`, 화자 라벨 `Sasha:`를 구조 키로 오판하던 문제 수정
- 펜스 언어를 판별해 `md`, `markdown`, `mb`, `text`의 사람이 읽는 라벨과 값을 정상 번역
- 실제 `yaml`, `yml`, `json`의 기계 판독용 키만 구조 키로 판정
- 모델이 YAML/JSON 키나 콜론 뒤 공백을 바꿔도 번역된 값은 유지하고 원문 키만 자동복원
- 캐시 결과와 구조 토큰 호환 재시도 결과에도 같은 키 자동복구 적용
- 코드 펜스·태그·줄 수·들여쓰기·빈 줄·블럭 위치 검사는 그대로 유지
- 실제 사용자 로그의 `mb + md` 2블럭 및 프로필 재시도 경로를 회귀 테스트로 추가

beta.4는 Markdown 상태블럭의 시간과 라벨을 키로 오판할 수 있으므로 beta.5로 교체하세요.

---

## v1.0.5-beta.4 — 입력 검증·채팅 전환 경합 수정

> 교체 권장: Markdown 상태블럭 키 오판은 beta.5에서 수정되었습니다.

- 정상 입력 번역의 `mes === display_text` 상태를 형식 붕괴로 오판하던 beta.3 검증 조건 수정
- 벌크 번역 도중 채팅을 이동하면 진행 중 요청을 중단하고 다음 메시지를 시작하지 않도록 보강
- 번역 히스토리를 고르는 동안 원문·번역문·스와이프가 바뀌면 낡은 선택 결과 적용 취소
- 원문 수정 감지 뒤 예약된 재번역이 다른 채팅의 같은 메시지 번호에 적용되지 않도록 보강
- 번역문 편집 도중 채팅이나 스와이프가 바뀌면 편집 결과 적용 취소
- 수기 번역문을 현재 스와이프 번역에도 함께 저장해 이전 번역이 되살아나는 문제 방지
- 모바일 직접 편집 저장의 500ms 지연 작업에도 시작 채팅 참조 고정
- LLM 응답창에 남을 수 있는 전역 `dialog` 변형을 제거하고 실제 표시 영역 기준으로 폭 계산

beta.3는 정상 입력 번역을 오판할 수 있어 폐기합니다. 최신 beta.5 테스트판을 사용하세요.

---

## v1.0.5-beta.3 — 정식판 분리·비동기 안정성 보강

> 폐기됨: 정상 입력 번역 상태를 형식 붕괴로 오판하는 경합 결함이 있어 배포하지 않습니다.

- 설치 폴더, 내부 이름, 설정 키, IndexedDB 캐시를 정식판과 분리
- 확장 목록과 설정창 이름을 `🙀 Translator Beta`로 고정
- 정식판이 활성화된 상태에서는 베타 로드를 중단해 이중 번역 방지
- 채팅 이동 전 예약된 저장·검증·자동 번역 작업 취소
- 번역 완료 뒤 정상 메시지를 반복 재렌더링하던 검증 동작 제거
- 번역 중 채팅, 스와이프, 입력창, 편집창 내용이 바뀌면 낡은 결과 폐기
- 편집 번역 성공 전에 기존 번역 상태가 삭제되던 동작 수정
- Scene Board 번역도 적용 직전 최신 채팅·원문 상태 재확인

정식판과 베타는 함께 설치할 수 있지만 한 번에 하나만 활성화하세요.

---

## v1.0.5-beta.2 — 번역 상태·언어 판정 안정화

이번 버전은 정식 배포 전 베타 테스트판입니다. 안정판과 함께 배포해 테스트 사용자만 사용하세요.

- 영어 입력을 한국어로 잘못 바꿔 AI 문맥을 오염시키던 입력 방향 판정 수정
- 한국어 고유명사/OOC/매크로가 섞인 영어 원문의 언어 오판 완화
- 번역 적용 뒤 영어 원문으로 돌아가는 비동기 상태 경합 보강
- 캐시 키에 원문 줄바꿈·문장부호·코드 구조와 대화 문맥을 반영
- 캐시 결과도 구조·모드 검사를 통과한 경우에만 적용
- 영문 원문을 문맥으로 수집하고 한국어 번역본은 말투 참고로만 분리
- 말투 혼용·미번역·사전어 누락은 품질 재시도 후 더 나은 결과 선택
- 일반 번역에 직역 병기/검증 과정이 새어 나오는 결과 차단
- 활성 시스템 프롬프트를 기능별 조건부 규칙으로 축약
- 모바일 LLM 응답창을 실제 visual viewport 안에 고정

검증: 프로필, Gemma/NanoGPT 호환 fallback, Gemini 직접 연결, Vertex 3.5, 직역/대사 병기, 코드블럭 2개, IndexedDB 캐시, 모바일 응답창 회귀 테스트 통과.

---

## v1.0.5-beta.1 — 구조 보존/응답창 수정 테스트판

이번 버전은 정식 배포 전 베타 테스트판입니다.

- 번역 형식 검증 실패 시 호환 fallback 1회 재시도
- 코드블럭/인포블럭 구조 보존 검증 강화
- 직번역 병기 오염 방지 보강
- 마지막 LLM 응답/에러 로그 창 모바일 잘림 완화

---

## v1.0.4 — 원문/번역문 편집 & 원문 보호 강화

### ✨ 새 기능

**원문/번역문 편집 분리**
번역된 메시지 이름 옆에 🐟(고양이)/🍖(호랑이) 편집 버튼이 추가됩니다.
- ST 연필 ✏️ → 원문(영어) 편집
- 🐟/🍖 → 번역문(한국어) 편집
- 번역문 수정 후 저장해도 영어 원문이 자동 보존됩니다
- 편집 취소 시 번역문이 자동으로 복원됩니다

### 🔧 버그 수정

**원문 오염 자동복구**
ST 내부 렌더링 과정에서 번역문이 원문을 덮어쓰는 현상에 대한 자동 복구 워치독을 추가했습니다. 로드 시, 채팅 전환 시, 10초 간격으로 상시 감시하여 오염 감지 즉시 복구합니다.

**에러 알림 한국어화**
API 오류 메시지가 한국어로 표시됩니다.
- 🔑 API 키가 유효하지 않아요 (401)
- 🚫 API 접근 권한이 없어요 (403)
- ⏳ 요청이 너무 많아요 (429)
- 💥 서버 오류가 발생했어요 (500)

**벌크 이모지 순서 수정**
🦁20개 → 🐯15개 → 🐱10개 → 🐭5개 순서가 올바르게 표시됩니다.

---

## v1.0.3 — 번역 품질 대폭 개선 & 설정 안정화

### 🔧 버그 수정

**설정 초기화 버그 수정**
채팅 전환 시 스타일/온도/추가지시사항이 초기화되는 버그를 수정했습니다. 전역 기본 설정이 영구 보존되며, 프리셋이 기본 설정을 오염시키지 않습니다.

**사전 오적용 방지**
사전에 등록된 단어가 관련 없는 캐릭터에 잘못 적용되는 현상을 수정했습니다. 본문에 실제 존재하는 단어만 프롬프트에 전송합니다.

### ✨ 새 기능

**번역 스타일 4종 추가**
기존 3종(일반/소설/캐주얼)에 4종이 추가되었습니다.
- 번역체 탈피 — 자연스러운 한국어 어순
- 존댓말 고정 — 해요체 구어
- 반말 고정 — 친구 말투
- 문어체 — 출판 소설풍

**벌크 번역 병렬화**
동시 2개 메시지를 병렬 처리하여 벌크 번역 속도가 약 2배 향상되었습니다.

**사전 이중 안전망**
API 호출 전 고유명사를 자동 치환하고, 프롬프트에도 Glossary를 동시 전달하여 사전 적용률이 크게 향상되었습니다.

### 📈 번역 품질 개선

**systemInstruction 분리**
Gemini API의 systemInstruction 필드를 활용하여 시스템 프롬프트가 번역 대상으로 오인되는 현상을 방지합니다.

**CONTENT FIDELITY 규칙**
- 원문에 없는 내용 추가 금지 (ZERO ADDITION)
- 문장 빼먹기/요약 금지 (ZERO OMISSION)
- 감정 강도 100% 보존 (EMOTION INTENSITY)
- 물리 동작 구체성 유지 (ACTION PRECISION)
- 캐릭터 말투 일관성 유지 (SPEECH CONSISTENCY)

**스타일별 few-shot 예시**
각 번역 스타일에 실제 번역 전/후 예시가 내장되어 번역 정확도가 향상되었습니다.

**문맥 화자 정보**
이전 메시지에 화자 이름(캐릭터/유저)을 포함하여 캐릭터별 말투 일관성이 향상되었습니다.

---

## v1.0.2 — 캐릭터별 프리셋 & 편집 모드 호환

### ✨ 새 기능
- 캐릭터별 번역 프롬프트 프리셋 생성, 저장, 삭제 기능 추가
- 캐릭터와 프리셋 연결(🔗) 기능 — 캐릭터 전환 시 연결된 프리셋 자동 로드
- 그 외 잔버그 수정

---

## v1.0.1 — 버그 수정 & 스마트 감지 강화

### 🔧 버그 수정

**스와이프 번역 덮어쓰기 수정**
스와이프(< >)로 다른 대사를 선택한 뒤 번역하면 이전 대사의 번역이 나오던 문제를 수정했습니다.

**수정창 캐시 꼬임 수정**
수정 모드에서 텍스트를 변경한 뒤 번역하면 변경 전 문장의 캐시가 나오던 문제를 수정했습니다.

**사전 단어 원문 병기 방지**
사전에 `Soap=소프`를 등록하면 번역 결과가 `소프[Soap]`처럼 원문이 함께 출력되던 현상을 수정했습니다.

### ✨ 새 기능

**일본어 · 중국어 스마트 감지**
기존 한국어 ↔ 영어 자동 감지에 더해, 일본어와 중국어 텍스트도 자동으로 감지하여 설정한 목표 언어로 번역합니다.

### 💡 사전 활용 팁: 동일 단어 다중 등록

같은 단어에 여러 뜻을 등록할 수 있습니다! AI가 문맥을 읽고 적절한 번역을 자동 선택합니다.

```
Soap=소프
Soap=비누
```

→ "Soap handed the knife" → **소프**가 칼을 건넸다 (캐릭터명)
→ "wash with soap" → **비누**로 씻다 (물건)

---

## v1.0.0 — 정식 출시 🎉

베타에서 정식 버전으로 전환! 주요 기능:

- 🐱/🐯 고양이·호랑이 테마 자동 전환
- Gemini / Vertex AI 직접 연결
- 사전 시스템 + 드래그 등록
- 모델별 캐시 분리 + 히스토리
- 전체(벌크) 번역 + 자동 번역 모드
- HTML/코드블록/YAML 구조 보존 번역
- 자동 저장 + 알림 스택
- 설정 내보내기/가져오기
